#------------------------------------------------------------------------------
# Token JWT. 
# Debe ser el mismo en todos los servers donde se despliegue, para poder 
# mantener la misma key entre servers, ante una caida de alguno de ellos
auth_secret_key: "pyapiafip-SeCrEt-KeY"

# -----------------------------------------------------------------------------
# Configuración del Nonce
# nonce_secret_key: clave para generar el Nonce.
nonce_secret_key: "pyapiafip-Secret-Nonce-Key"
